# 추가 작업 스펙 — 갱신 시각 변경 + 프로토 승부식(축구) 페이지

기존 proto-calc 리포에 이어서 작업합니다. `index.html`(승무패 14경기)은 그대로 두고,
아래 두 가지를 추가/수정해 주세요.

---

## A. 자동 갱신 시각 변경

`.github/workflows/update.yml`의 schedule을 **KST 09:00 / 19:00**으로 바꿉니다.

- cron: `0 0 * * *` (KST 09:00), `0 10 * * *` (KST 19:00)
- 기존 KST 07:00(`0 22 * * *`)은 제거.
- 이유: 토토·프로토 새 회차 발매는 통상 오전 8시경 시작이라, 07시 실행은 매번 헛돈다.
- `workflow_dispatch`는 유지.

---

## B. 프로토 승부식(축구) 배당 조합 계산기 추가

### B-1. 페이지 배치

- **별도 페이지를 만들지 않습니다.** 첨부된 `index_merged.html`이 기존 승무패 계산기와
  승부식 계산기를 한 페이지에 합친 최종본입니다. 이 파일로 리포 루트의 `index.html`을
  **통째로 교체**하세요. (바이트 단위 그대로. 버그가 아닌 이상 수정 금지, 발견 시 고치기 전에 보고)
- 주소는 기존 그대로 `https://<계정>.github.io/proto-calc/` 하나뿐입니다.
- 페이지 위쪽에 두 섹션(`#sw`, `#pz`)으로 건너뛰는 링크가 이미 들어 있습니다.
- 기존 `index.html`은 `archive/index_v1.html`로 옮겨 보관하세요.

### B-2. data/proto.json 스키마 (프런트가 기대하는 형식 — 반드시 준수)

```json
{
  "round": "101",
  "deadline": "8/30(일) 20:00",
  "updated": "2026-08-28T19:00:00+09:00",
  "updated_label": "8/28 19:00",
  "games": [
    {
      "no": 12,
      "league": "EPL",
      "home": "맨시티",
      "away": "첼시",
      "kickoff": "8/30(토) 21:00",
      "odds": { "승": 1.55, "무": 4.10, "패": 5.20 }
    }
  ]
}
```

- `no`는 **베트맨 화면에 표시되는 경기번호 그대로**. 1부터 다시 매기지 말 것.
  (마킹지에 이 번호를 적기 때문에 원본 번호가 유지되어야 함)
- `odds` 값은 숫자(float). 해당 항목이 없으면 키를 생략하거나 null.
  프런트는 값이 없는 항목 버튼을 자동으로 비활성화함.
- `updated_label`은 배당 기준 시각의 짧은 표시용 문자열(KST).
- `deadline`은 회차 마감 표시용 문자열. 없으면 생략 가능.

### B-3. 어떤 경기를 담을 것인가 (중요)

- **프로토 승부식**의 대상경기 중 **축구**, **일반 유형(승/무/패)만**.
- 제외: 야구·농구·배구 등 타 종목, 핸디캡, 언더오버, 승1패/승5패, 기록식, 기타 유형.
  (같은 경기의 일반/핸디캡/언더오버는 규정상 교차 조합이 안 되므로 섞으면 계산이 틀려짐)
- 마감이 이미 지난 경기는 제외. 발매중인 것만.
- 경기 수는 회차에 따라 수십 개가 될 수 있음. 상한을 두지 말고 전부 담되,
  `kickoff` 빠른 순 → 같은 시각이면 `no` 순으로 정렬.

### B-4. 수집 스크립트

- `scripts/fetch_proto.py`로 분리(기존 `fetch_games.py`는 승무패 전용으로 유지).
  공통 HTTP 로직(세션, `_sbmInfo` 봉투, 재시도)이 있으면 모듈로 빼서 양쪽이 같이 쓰도록.
- 기존 승무패 수집에서 확인한 베트맨 내부 XHR 방식을 그대로 활용.
  프로토 승부식은 gmId가 다르므로 `inqScheduleSltList.do` 등으로 먼저 확인할 것.
- **검증**: 경기가 0개거나 배당이 전부 비면 실패로 처리하고 기존 `data/proto.json`을
  덮어쓰지 않는다 (워크플로는 exit 0으로 성공 처리, 로그에 사유 명시).
- 내용이 기존과 같으면 파일을 다시 쓰지 않음.
- 연결 오류·타임아웃은 기존과 동일하게 재시도.
- 같은 워크플로에서 `fetch_games.py` → `fetch_proto.py` 순차 실행. 한쪽이 실패해도
  다른 쪽 결과는 커밋되어야 함.

### B-5. 확인 기준

1. 사이트 접속 시 위쪽에 승무패 14경기 계산기가, 아래쪽 "축구 전경기 배당 계산기" 섹션에
   승부식 경기 목록과 각 경기의 승·무·패 배당이 보인다.
2. 경기를 2개 이상 고르면 하단에 조합 수·총액이, 본문에 조합별 배당과 예상 환급금이 나온다.
3. `workflow_dispatch` 수동 실행으로 `data/proto.json`이 생성/갱신된다.
4. README에 proto.json 스키마와 수동 수정 방법을 추가 (주소는 기존과 동일).

### B-6. 하지 말 것

- 배당을 반올림해서 저장하지 말 것(원본 소수점 그대로).
- 경기번호 재부여 금지.
- 축구 외 종목이나 핸디캡/언더오버를 섞어 담지 말 것.
- 과도한 크롤링(스케줄 외 반복 요청, 병렬 요청).
